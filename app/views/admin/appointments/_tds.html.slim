td = appt.user.lname
td = appt.user.fname
td = appt.day.date if appt.day
td = am_pm appt.slot_time if appt.day
td = appt.user.college
td = appt.user.class_year
td = link_to appt.user.email, edit_user_path(appt.user)
/td = appt.user.phone
td = appt.double_red?
td = link_to appt.user.netid, edit_user_path(appt.user)
td
  - if appt.last_reminded_at
    .timestamp
      = time_vs_now(appt.last_reminded_at) 
  - else
    = button_to "remind", remind_drive_appointment_path(appt.drive, appt), class: 'btn btn-warning btn-xs', remote: true
td = link_to 'edit', edit_drive_appointment_path(appt.drive, appt), target: '_blank'
